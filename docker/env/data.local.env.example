# Kopiere nach data.local.env. Niemals echte Tokens committen.
ENTSOE_API_KEY=
# SMTP fuer `pipeline.status --send`:
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=
SMTP_TO=
SMTP_STARTTLS=true
